import { Center } from '@mantine/core';
import { ReactComponent as IconPerson } from '../assets/images/Person.svg';

export const Empty = () => {
  return (
    <Center>
      <IconPerson />
    </Center>
  );
};
